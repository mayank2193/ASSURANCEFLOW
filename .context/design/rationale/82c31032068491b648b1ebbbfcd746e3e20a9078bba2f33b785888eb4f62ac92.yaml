anchor: ac-14
combinatorial:
  reason: 'Techniques: state_flow for homepage-to-results navigation; eqp for the explicit no-dates path and typed destination classes. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. The omitted-dates path is a source-defined supported variation, so it is modeled as its own scenario rather than a negative case.'
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: 'Techniques: state_flow for homepage-to-results navigation; eqp for the explicit no-dates path and typed destination classes. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. The omitted-dates path is a source-defined supported variation, so it is modeled as its own scenario rather than a negative case.'
