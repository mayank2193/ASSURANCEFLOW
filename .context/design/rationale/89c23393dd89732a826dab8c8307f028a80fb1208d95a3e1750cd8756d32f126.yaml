anchor: ac-9
combinatorial:
  reason: 'Reused existing scenario coverage instead of minting a narrower duplicate because a fresh keyboard-accessibility scenario already covers the required results-page controls, even though it also spans homepage focus order. Techniques: a11y for keyboard reachability and visible focus state; state_flow because the path spans homepage routing into results-page refinement. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. No budget ceiling was set, so this path was kept.'
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: 'Reused existing scenario coverage instead of minting a narrower duplicate because a fresh keyboard-accessibility scenario already covers the required results-page controls, even though it also spans homepage focus order. Techniques: a11y for keyboard reachability and visible focus state; state_flow because the path spans homepage routing into results-page refinement. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. No budget ceiling was set, so this path was kept.'
