anchor: ac-27
combinatorial:
  reason: 'Reused existing scenario coverage instead of minting a duplicate because an identical fresh scenario already exists. Techniques: pagination/search-list filtering profile for results refinement; state_flow because the path starts from routed search results. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. No budget ceiling was set, so this path was kept.'
  strength: 2
deferred_bindings: 0
expected_how: 'expected_mode: authored'
profile_family: unspecified
surface_state: absent
technique: 'Reused existing scenario coverage instead of minting a duplicate because an identical fresh scenario already exists. Techniques: pagination/search-list filtering profile for results refinement; state_flow because the path starts from routed search results. Risk-judged interaction strength = 2 because no money, auth, or data-loss promise applies. No budget ceiling was set, so this path was kept.'
