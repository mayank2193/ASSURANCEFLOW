---
mode: testing
max_steps: 30
timeout: 300
variables: {}
---

# Perform search with destination and guests only and verify results page

## Step 1
Perform search with destination and guests only.

Search with destination "Goa, India" and 2 adults, leaving the **Check-in** and **Check-out** date fields empty.
Verify the search header on the results page displays the destination.
Verify the search header on the results page displays the guest count.
Verify the date fields in the header prompt for input.
